# workflow-prototype

Minimal `React + Vite` starter with automatic GitHub Pages deployment on every push to `main`.

## Scripts

- `yarn dev`
- `yarn build`
- `yarn preview`

## Deploy

GitHub Actions builds the app and deploys `dist/` to GitHub Pages from `.github/workflows/deploy.yml`.
