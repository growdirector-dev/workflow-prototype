const App = () => {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">React + Vite + GitHub Pages</p>
        <h1>Hello world.</h1>
        <p className="lead">
          This page is built with Vite and deployed automatically on every push
          to <code>main</code>.
        </p>
      </section>
    </main>
  );
};

export default App;
